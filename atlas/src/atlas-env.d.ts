/** Defined by `vite.config.ts` (`command === 'serve'`) and by `vitest.config.ts`.
 *  True only when the committed development fixture may stand in for the real
 *  bundle. See `src/lib/load.ts`. */
declare const __ATLAS_ALLOW_FIXTURE__: boolean
